server:
  port: 10002
eureka:
  server:
    hostname: localhost
    port: 10000
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://${eureka.server.hostname}:${eureka.server.port}/eureka/
spring:
  application:
    name: eureka-client-10002